if parser == "problems":
    for problem in data:
        if problem.get("recentComments"):
            print("\n===== FOUND A COMMENT =====")
            print("Problem ID:", problem.get("problemId"))
            print("Recent Comments:", problem.get("recentComments"))
            print("===========================\n")
            break
